apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: devops-observability
  labels:
    app: prometheus
    component: monitoring
spec:
  selector:
    app: prometheus
  ports:
  - name: http
    port: 9090
    targetPort: 9090
    protocol: TCP
  type: ClusterIP
