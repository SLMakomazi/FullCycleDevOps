apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  namespace: devops-observability
  labels:
    app: devops-api
    component: backend
spec:
  selector:
    app: devops-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
