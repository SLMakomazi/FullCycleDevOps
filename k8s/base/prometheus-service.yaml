apiVersion: v1
kind: Service
metadata:
  name: fullcycle-api-metrics
  labels:
    app: fullcycle-api
    monitor: prometheus
spec:
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
  selector:
    app: fullcycle-api
